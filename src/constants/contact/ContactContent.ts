import { Language, ContactContent } from "@/types";

export const contactContent: Record<Language, ContactContent> = {
  es: {
      title: "Contacto",
    },
    en: {
      title: "Contact",
    }
}